import { wp } from "@/utils/ui/sizes";

const Sizes = {
  containerPaddingHorizontal: wp(4),

  // Radius
  extraSmallRadius: 4,
  smallRadius: 8,
  mediumRadius: 12,
  largeRadius: 16,
  extraLargeRadius: 24,
};

export default Sizes;
